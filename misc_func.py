# Various small functions needed for Xfs


def IsAlpha(text):
    if (text.isalpha()):
        return True
    else:
        return False

def IsNum(text):
    if (text.isdigit()):
        return True
    else:
        return False
